[project]
name = "scripts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "foamlib>=1.1.1",
]

[dependency-groups]
dev = [
    "ipython>=9.4.0",
    "matplotlib>=3.10.3",
    "pandas>=2.3.1",
]
